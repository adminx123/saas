name = "orchestrator"
main = "orchestrator.js"
compatibility_date = "2024-01-01"

# KV for usage tracking - commented out for local dev
# kv_namespaces = [
#   { binding = "USAGE_KV", id = "your-kv-id", preview_id = "your-preview-kv-id" }
# ]

# Service bindings to client config workers - commented out for testing
[[services]]
binding = "CONFIG_INEXASLI"
service = "config-inexasli"